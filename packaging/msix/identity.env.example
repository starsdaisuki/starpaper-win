# 从 Partner Center 的 Product management → Product Identity 抄过来，
# 然后 `cp identity.env.example identity.env` 填进去。identity.env 不入库。
# ⚠️ 三个值必须与 Partner Center 逐字一致（大小写、空格、标点都算）。
IDENTITY_NAME=YourPublisher.YourApp
IDENTITY_PUBLISHER=CN=00000000-0000-0000-0000-000000000000
PUBLISHER_DISPLAY_NAME=YourPublisherDisplayName
